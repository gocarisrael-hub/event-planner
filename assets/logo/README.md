# Ocar logo

Drop the Ocar logo file here, named **`ocar-logo.png`** (or `.svg` / `.jpg`).

After adding it, also copy it to `server/assets/ocar-logo.png` so the proposal page can show it
(or just tell me and I'll wire it up). Until then a placeholder wordmark is used.

You can also set the brand colors in `client/src/brand/brand.js`.
